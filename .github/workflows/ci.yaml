# This builds + pushes your Docker image automatically.

name: CI

on:
  push:
    branches: ['main']

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: yashmauryadev
          password: dckr_pat_tqKeFmvIgtdvF0m2cpA9PozO3ns

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: yashmauryadev/nest-devops-todo-api:latest
